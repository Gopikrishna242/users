@Library('todo') _

todo (
        COMPONENT             : 'users',
        PROJECT_NAME          : "TODO",
        SLAVE_LABEL           : "node1",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "JAVA"
)
